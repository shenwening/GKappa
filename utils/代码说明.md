1.function [sequence1, sequence2] = generate_similar_unequal_category_sequences(vectorLength, n, rho, s_tho)
Generate two sequences where one sequence contains a certain category occupying proportion rho, and the other sequence has similarity s_tho to the first.

2.function sequence = generateSequence(startVal, endVal, numSamples)
Generate a random sequence.

3.function sequence = generateSequence_proportions(scale, proportions, totalSamples)
Generate a sequence containing samples of different categories, with category distribution controlled by proportions.

4.function newSequence = generateCorrelatedSequence(startVal, endVal, sequence, rho)
    % startVal - lower bound of sequence values
    % endVal - upper bound of sequence values
    % sequence - original sequence
    % rho - correlation proportion with the original sequence (0 <= rho <= 1)

5.function outputArray = generateSpecialArray(startVal, endVal)
    % startVal - left interval bound
    % endVal   - right interval bound
    % Output: an array of length (endVal - startVal + 1)
    %         mostly zeros, with only three consecutive elements summing to 1
