sequence = generateSequence(1, 10, 10)

newSequence = generateCorrelatedSequence(1, 10, sequence, 0.6)

outputArray = generateSpecialArray(1, 10)
scale = 1:10;
sequence = generateSequence_proportions(scale, outputArray, 10)

